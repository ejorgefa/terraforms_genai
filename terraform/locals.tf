locals {
  environment = var.stage
  # client_application_name = "${var.client_name}-${var.application_name}"
  # storage_account_name = "st${var.client_name}${var.application_name}${local.environment}"
}
